import React from "react";

// Deprecated: Navigation is now integrated into Header. This component returns null to avoid duplicate nav bars.
const Navbar: React.FC = () => null;

export default Navbar;
